# 今日复盘与明日推演：闭环改进实施计划

用户已在本任务确认上一轮方案；沿用既有数据门禁、仓位阈值与策略类型，不引入自动下单。

## 范围与验收

1. [完成] 候选唯一来源：新增纯函数候选漏斗，保留全部观察/拒绝原因；主线精确匹配，先排序再截取；情景、看板、CSV、审计使用同一对象；CSV保留候选的优先级与执行契约。
2. [完成] 有效情景：区分最高排名、有效情景、可决策情景；硬失效优先；必需字段不足不得确认；所有失效明确禁止新仓。
3. [完成] 涨停事件字段：保留来源中实际存在的封板时间、开板次数、资金、换手字段，区分收盘涨停子集与全体尝试；不伪造一字板/全市场炸板率；保存有日期与来源的独立事件快照。
4. [完成] 盘中验证：修正丢失盘后基准问题；校验报告日/交易日/时区/时段/来源与质量；不允许历史或错误时段快照激活当前计划；提供显式本地导入入口，不暗中启用定时任务。
5. [完成] 结果闭环：每天记录决策（包括不交易），保留计划修订与状态迁移；结果未知可追加后续确认，净收益需显式给定币种/口径；复盘覆盖历史计划而不只当天；新增决策变化摘要。
6. [验收中] 验证交付：新测试先失败再实现；全量pytest、Python 3.10兼容、差异检查；真实历史快照离线渲染与内容对照；文档说明已实现与外部依赖，不把数据未接入写成完成。

## 文件职责
- candidate_funnel.py：唯一候选事实、资格与排序。
- scenario_posterior.py / decision_readiness.py：情景状态与行动许可，保留门禁。
- data_sources/limit_pool_provider.py / limit_events.py：原始字段保留、覆盖说明与事件归档。
- phase_monitor.py / market_snapshot.py：盘中输入校验、日期隔离和基准绑定。
- trade_plan_review.py：版本化决策与计划事件、结果迁移和历史复盘。
- decision_dashboard.py / 主线强度追踪.py：装配、发布和展示，不各自重选候选。
- tests/test_recap_scenario_closure.py：贯穿上述链路的语义验收。

## 边界
不更换AI模型或密钥；不取消AI/历史/炸板既有门禁；不批量重抓、不改远程分支；未提供的实时数据、成交记录、持仓明确保持未知。

## 2026-09-07 续接记录
- 已修复上轮机械替换丢失的情景函数返回与选择接口；新增硬失效、必需字段和显式空值的回归。
- 已复现并修复盘中监控过滤掉收盘基准、已有快照ID绕过校验、错误日期/时段/来源也能确认的问题；补充严格目标日绑定。
- Ruling: 沿用已获用户同意的现有 master 工作区，不搬移或覆盖用户与其他任务的未提交数据。
- Ruling: 目标日只读既有交易日历缓存；缓存不足保持未绑定，不用“下一个工作日”猜节假日。
- Ruling: 旧情景排名可作历史展示，但无日期/来源/质量证据的盘中支持不再构成执行确认；回归夹具补齐真实契约字段而非保持不安全行为。
- 每日决策/计划结果模块已交接，独立审查进行中；报表/监控接入与全量验收尚未完成。

## 验收证据（2026-09-07）
- 候选：新增同主线排序、截断前排序、个股归因缺失拒入、情景候选子集、CSV不放行其余对象；风险锚与未采用对象可折叠核查。
- 情景：硬失效不被正分覆盖，必需字段/未满足触发分离；同阶段与跨阶段失效留痕，新坏数据不清空旧失效；先验/条件命中率分离。
- 盘中：目标交易日、上海时区/时段、来源/质量、预测修订ID绑定；旧未绑定记录不确认；收盘基准保留。显式CLI不抓数、不调度、不下单。
- 事件：Source→Provider→旧表旁路attrs→每日归档→主报告审计已接通。保留qdate和原始证据，0/False不当缺失，收盘池不是全市场尝试，既有bomb门禁不变。
- 账本：每日记录（含不交易）、同逻辑计划修订、迟到结果按修订归属；已知收益需来源/币种/口径；歧义与未归属显式显示，不丢失、不猜交易笔数。
- 独立审查：涨停字段3项反例、账本6+2项反例、集成3项反例均落测试并修复；账本最后4组反例、集成11项检查通过。
- 全量：最近968 passed，3条既有pandas FutureWarning；新增保留窗口回归27 passed。合并后还需复跑全量。
- 兼容：src/tools/tests共157个Python文件可按Python 3.10语法解析；实际全量运行于3.13。纯Python账本105项在3.11也通过，不把此结果说成3.10全量。
- 实景：2026-09-04审计离线重算，目标2026-09-07；行情通过/策略尚未验证/信号不可评估/不开新仓。36行CSV全部不可执行；三种HTML与CSV候选指纹一致，原质量不变；仅展示2个观察对象+2个风险锚，32个其余折叠。
- 浏览器：完整和紧凑预览均检查，390px宽无横向溢出。预览与模拟日志只在output/recap_validation，不改真实历史成交。
- 发现旧test_lianban_dates会把真实日历写成2天夹具；已隔离其文件路径，并从原权威源恢复8797日缓存。没有改代理/API Key/AI模型。
- Git远程连通正常；origin/master新增bd804c4仅为缓存提交。待合并时保留本地+远程不同快照/预测记录，禁止force push。
