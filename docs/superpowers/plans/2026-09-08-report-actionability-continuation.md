# 报表行动性优化：原始总纲与续接验收（2026-09-08）

## 不变的总纲
原任务：分析报表并提出买入指引优化建议（01a07241-713d-7b50-8162-90912539d0a9）。以2026-09-06最初审查方案为总纲，而非不断增加图表或另建策略。
1. P0 统一事实/晋级统计；分开不能判断、判断后不买、等待确认；说明有效期与恢复条件。
2. P0 主线先筛后排再截取；首选1只、备选最多2只，允许0只，风险锚不混入执行池。
3. P1 唯一结构化计划驱动首页、主报告、看板、CSV、审计。
4. P1 逐票真实价格条件、时间窗口、风险预算、撤销条件；不伪造持仓，尊重A股T+1。
5. P2 计划触发、可成交性、真实成交及费用后收益/回撤分开记录；情景命中率不是交易胜率。

原始审查：docs/audits/2026-09-06-report-actionability-review.md。
已合并基础：7ef3194；本轮继续原工作区codex/strategy-qualification-20260907上的未提交修改，不重置代码和真实行情。

## 当前收尾（原总纲的P0资格/P1确认链路，不扩大范围）
- [x] 恢复最初要求、已批准设计及最新交接，核验Git与数据改动。
- [x] 核验原3类事件与样本成熟时点修复；补齐事件交集下界、零尝试/零分母一致性；事件独立审查通过。
- [x] 核对逐策略依赖与盘后/盘中确认、HTML/CSV/审计一致性，保留核心行情硬门禁；旧版回放/默认预览不自动授权。
- [x] 全量禁网回归、Python3.10语法、真实9/4与9/7历史离线重放及HTML/CSV结构检查。
- [ ] 最后独立复核关闭F1/F2与F5 HTML残留（修复与回归已落盘）。
- [ ] 文档与证据归档；保留运行数据；按原用户同步授权正常提交、合并、推送并复核。

## 约束与裁定
- 无独立策略证据仍unverified；不得为了名单放宽门禁或伪造行情、样本、持仓、成交、收益。
- 不改模型/密钥/代理，不新增自动任务，不联网抓全量，不下单。历史预览不是实时买入建议。
- Ruling: 继续已批准、已有未提交成果的本地分支，不迁移/清空工作树，以免丢失真实运行数据。
- Ruling: 事件校验与样本时点修复按不相交文件并行；主线程负责成熟时间与集成，事件worker只改事件模块/对应测试。
- 原任务read_thread历史过长，已用限定该任务ID的本地JSONL提取用户消息、最初方案和交接，未扩大到其他任务。

## 验证记录
- 最新全量：1169 passed，3条原有pandas FutureWarning，40.47s；165个Python文件通过3.10 AST语法与compileall（未声称运行3.10全量）。
- 事件语义：原24例加相邻边界33例通过；独立审查spec/quality通过，0项残留；完整观测、缺失与无适用事件分开。
- 初次集成审查发现F1/F2/F3/F4/F5，均按反例补回归。F3/F4复核已关闭；F5曾发现紧凑HTML出口遗漏，8个新HTML反例现通过；默认legacy预览旁路也已补上。最终限定复核待回报。
- F1/F2有53个回放/序列化/权限上限回归；两条旧测试保留分析和计划修订的原意，使用未授权分析或现代匹配证据fixture，不恢复旧隐式授权。
- 历史源审计冻结于output/strategy_qualification_validation/resume-20260908/source_audits；缺少适用日的旧审计应为facts_only、0候选/0CSV交易行，不倒填通过。原始审计与核心模块值不改。
- 合成证据正控验证同一HTML/CSV：盘后wait_confirmation、盘中enter_plan。合成样本只在测试使用，未创建生产strategy_validation.json。
- 真实生产验证文件仍缺失；修复不是宣称策略已有赚钱能力。报价/持仓缺失仍明确未知，不编造入场价、收益或成交。
- 远端3个缓存提交已fetch至ffdeefd；本地真实日志/缓存已保存合并前副本，后续正常合并，不能强推或删除一侧记录。

## 补充视觉检查的限制
浏览器工具明确禁止本地file URL，因此停止该动作，不经其他浏览器或转发服务绕过。浏览器视觉检查未完成；原已批准功能规格的代码、数据与多输出一致性验收继续使用真实生成HTML的结构断言。不能把静态检查写成浏览器实测，最终交付需披露此限制。

## 原始总纲的证据映射
| 原始目标 | 验收证据 |
|---|---|
| 不能判断/判断后不买/等待确认分离 | test_decision_readiness.py；test_strategy_provenance.py；真实旧审计缺字段拒绝与合成合格计划正控 |
| 主线先筛后排；首选1、备选最多2，允许0 | test_recap_scenario_closure.py；test_report_rendering.py中的主线截断、非主线排除、1+2与0候选回归 |
| 同一计划驱动HTML、CSV、审计 | test_report_integration.py；test_strategy_qualification_integration.py；verify_historical_outputs.py |
| 不伪造价格/持仓，显式有效期与T+1 | test_execution_contract.py；CSV参考价保留及目标日回归 |
| 每天留痕，修订不造新成交，真实扣费结果单独回填 | test_daily_decision_journal.py；test_report_closure.py；test_trade_plan_review.py |
| 数据与策略资格不被AI整体捆绑，旧许可不可扩大 | test_strategy_provenance.py；test_phase_qualification_envelope.py；独立限定审查 |
