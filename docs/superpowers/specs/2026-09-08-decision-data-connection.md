# 数据接通与根因告警修复（用户已确认方向）

## 目标
沿用最初行动单总纲，完成用户明确要求的三件事：93条事件数据真正进入策略输入、真实盘中输入与真实样本外验证、同源告警只说一次。不以新增门禁或伪造通过代替补链路。

## 已确认根因
- 最近9/7原始事件记录93条；break_count/首封/末封/封单/换手/成交额/流通值均93/93；规范attempted/broken/reclosed/board_type全为null。
- limit_events原观察层有意不派生，资格层却要求规范状态。增加有证据的事实解释层，不改写原始观测含义。
- 主入口只在没有归档时建立事件快照，已存在但欠完整的快照被永久复用。
- tools/backtest_pnl_v2_1.py把涨跌家数归一化当收益proxy，不能作为交易净收益或样本外资格证据。

## 分层修复
1. 原始事件归档保留；新增resolve_limit_event_facts(snapshot, price_rows=None)，每个派生状态带规则版本、原值与来源。仅同日可信ZT/ZB成员与明确非负整数炸板次数可解释尝试/炸板/当前回封；DT不冒充上限事件。板型只用可验证的同日原始OHLC或明确供应商板型，不凭换手率猜测。
2. 事件归档在新同日观测更完整时刷新，保留已经知道的字段与修订证据；不得改权威成员、姓名、连板高度或跨日期/批次拼接。完整全市场尝试池须包含炸板未回封成员，不能把收盘涨停池更名为全市场。
3. 盘中采集复用现有行情客户端，返回来源日期、采集时刻、覆盖及真实阶段快照；不伪造已过去的竞价/9:35/10:00时点，不自动创建定时任务、不下单。
4. 样本外研究不复用广度代理P&L。必须核实历史数据与现有交易执行定义；缺失的入场/退出/成交模型属于必须明确的研究假设，不以简单收盘收益偷换个股策略验证，也不自动写passed。
5. 展示层按上游根因归并，保留机器明细：已有行情/未接事件/未观测阶段/独立验证未完成/研究分支不适用/AI服务失败分开，清楚列影响与恢复步骤。

## 约束
Python>=3.10，Windows PowerShell7/UTF8。不改模型、密钥、代理；不新增定时任务、不自动下单。不覆盖用户进入本轮已有的两份JSONL改动；真实数据检查单独保存证据。
