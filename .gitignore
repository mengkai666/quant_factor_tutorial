# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
*.egg-info/
*.egg
dist/
build/
*.spec
*.exe

# === 运行时生成的缓存数据 (过大，不适合存仓库) ===
# 这些文件在 GitHub Actions 中通过 actions/cache 管理
price_history_cache.csv
data/price_history_cache.candidate.csv
data/price_sample.candidate.csv
data/sample_universe.csv
industry_cache.csv
data/stock_universe.csv
data/fetch_status.csv
data/trading_calendar_cache.csv
data/price_gap_negative_cache.csv
data/limit_pool_cache_meta.csv
data/market_data_quality.json

# === 运行时生成的输出/报告 ===
# output/ 整目录为运行时产物 (报告/图表/回测CSV/日志), 零跟踪文件, 不入 master
output/
*.html
data_quality_report.json
focus_pool.csv
连板高度分析.xlsx
# 站点发布产物 (本地累积, CI 部署到 gh-pages 分支, 不入 master)
output/site/
# 架构图的 visual-check 复核产物 (PNG 渲染 + 判定 JSON, 由 .architecture.json 重跑可得,
# 单份近 2MB 二进制, 不入库; 图的**源规格** docs/*.architecture.json 照常跟踪)
docs/*.visual-check.*

# === IDE / 编辑器 ===
.vscode/
.idea/
*.swp
*.swo
*~

# === 系统文件 ===
Thumbs.db
.DS_Store

# === 临时/调试文件 ===
temp_*.py
ai_*.txt

# === 打包归档 ===
*.zip
backups/
.worktrees/

# === PyInstaller ===
*.manifest
*.spec

# === 虚拟环境 ===
venv/
.venv/
env/

# === 提取的归档 ===
_extracted_zip/

# === Pyrefly ===
pyrefly.toml

# === Claude Code 本地配置 (含权限设置, 不公开) ===
.claude/

# === 运行日志 ===
*.log
run_*.log

# === 本地密钥 (API key 等, 绝不入库) ===
.env
.env.*
!.env.example

# CSV 冲突标记自愈备份 (paths.py 运行产物, 不入库)
*.conflict.bak

# 运维脚本落库前的带时间戳备份 (tools/backfill_price_gap.py 等, 不入库)
*.csv.bak.*

# === 本机杂物 / 外部克隆 (绝不入库) ===
# %SystemRoot% 是 shell 变量未展开时误建的空壳目录; ai_quant_trade 是外部克隆 (1.7G)
%SystemRoot%/
.playwright-cli/
ai_quant_trade/
# 别的任务在本目录里落下的 docx 提取残渣 (内容与本项目无关), 不入库
source_extracted.*
source_paragraphs.txt
source_unescaped.txt

# === AI 研判输出缓存 (运行时产物, 按输入指纹命名, 不入库) ===
data/ai_output_cache/

# === 一次性排查脚本与当日临时快照 ===
data/analyze_today_*.py
data/today_*.json
data/preplan_candidates_*.json
